# Copy to .env.local and fill in privately. Never commit the real token.
TMDB_READ_ACCESS_TOKEN=

# Optional: public address of the local search server, never a TMDB token.
# Expo Go normally detects the computer address automatically on LAN.
# EXPO_PUBLIC_SEARCH_API_URL=http://YOUR_COMPUTER_IP:3001
