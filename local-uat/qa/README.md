# Local browser QA

Keep only maintained checks for the current browser application in this directory. Retire instructions when the related behaviour is removed or automated.

The historical `D:/Dev/showtime-qa` directory is intentionally not imported: it is outside this repository, is not Git-managed, and contains older generated evidence rather than the current application source of truth.
